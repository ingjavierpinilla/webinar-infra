apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-landing
  namespace: flag
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
      version: landing
  template:
    metadata:
      labels:
        app: my-app
        version: landing
    spec:
      containers:
      - name: my-app
        image: jmpinillag/color-test:0.0.1
        ports:
        - containerPort: 5001
        envFrom:
        - configMapRef:
            name: configmap-landing
